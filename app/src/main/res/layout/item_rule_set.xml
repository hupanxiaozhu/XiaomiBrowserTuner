<?xml version="1.0" encoding="utf-8"?>
<!-- 一行 = 一个规则集：启用开关 + 名称/元信息 + 查看/删除 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:descendantFocusability="blocksDescendants"
    android:gravity="center_vertical"
    android:minHeight="52dp"
    android:orientation="horizontal"
    android:paddingStart="10dp"
    android:paddingTop="6dp"
    android:paddingEnd="4dp"
    android:paddingBottom="6dp">

    <CheckBox
        android:id="@+id/cb_rule_on"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="4dp"
        android:paddingEnd="4dp">

        <TextView
            android:id="@+id/tv_rule_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/mx_text_primary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tv_rule_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="2dp"
            android:textColor="@color/mx_text_secondary"
            android:textSize="11sp" />
    </LinearLayout>

    <Button
        android:id="@+id/btn_rule_view"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:minWidth="0dp"
        android:text="@string/rule_mgr_view"
        android:textColor="@color/mx_accent"
        android:textSize="13sp" />

    <Button
        android:id="@+id/btn_rule_delete"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:minWidth="0dp"
        android:text="@string/rule_mgr_delete"
        android:textColor="@color/mx_accent"
        android:textSize="13sp" />

</LinearLayout>
